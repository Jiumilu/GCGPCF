---
doc_id: GPCF-DOC-29B08242D5
title: 知源专项首轮实施前验证包
project: GPCF
related_projects: [GPCF, GFIS, WAES, KDS, Brain]
domain: workstreams
status: controlled
version: v1.0
owner: GPCF
kds_space: 开发
kds_path: 开发/12-GPCF/06-workstreams/知源专项首轮实施前验证包.md
source_path: 06-workstreams/知源专项首轮实施前验证包.md
sync_direction: bidirectional
last_reviewed: 2026-06-23
supersedes: []
superseded_by: []
---

# 知源专项首轮实施前验证包

日期：2026-06-08
专项：知源
范围：KDS 知识主存与知识引擎域
当前状态：`in_progress`

## 1. 当前验证目标

1. 补齐 Brain remote 证据
2. 抽取 ingest、发布、引用回指、失效拦截的实现落点
3. 锁定 A19 验收包的最小运行前样本要求

## 2. 当前边界

1. 本包覆盖 KDS 知识主存与知识引擎治理域。
2. 本包不把 `LLM Wiki` / `Brain` 写成企业级知识真源。
3. 本包只定义运行前样本，不做知识发布闭环完成判断。

## 3. 当前工作环境

1. 项目仓库环境：`/Users/lujunxiang/Projects/GlobalCloud Brain`
2. 设计控制环境：`/Users/lujunxiang/Documents/GlobalCloud智慧工厂`

## 4. 推荐工具与技能

推荐工具：

1. Shell
2. Git
3. 文档工具

推荐技能：

1. `globalcloud-harness-governance`

## 5. 验证方法

1. 知识治理样本法
2. 引用回指样本法
3. 验收包拆解法

## 6. 当前必收样本

1. remote 证据
2. ingest 样本或实现落点
3. 发布样本或实现落点
4. 引用回指样本或实现落点
5. 失效拦截样本或实现落点

## 7. 建议只读动作

1. 在 Brain 仓库继续抽取知识治理脚本与 Harness 文档落点
2. 固定 Brain doctor、search、trust audit、rag admission 的验证入口
3. 形成 A19 的专项验证清单

### 7.1 本轮已形成的样本记录

已形成独立样本记录：

1. [知源专项运行前样本收集记录](./知源专项运行前样本收集记录.md)

当前已确认：

1. remote 当前无返回，缺口仍存在
2. ingest 入口已明确：
   - `gbrain import . --no-embed`
   - `gbrain extract links --source db`
   - `gbrain extract timeline --source db`
3. 回指与准入校验入口已明确：
   - `rag_admission_policy.py`
   - `wiki_trust_audit.py`
   - `validate_rag_export.py`
4. 失效拦截入口已明确：
   - `validate_evidence_gates.py`
   - `RAG_ADMISSION_RULES.md`
5. A19 组合验证入口已明确：
   - `gbrain search`
   - `gbrain doctor --json`
   - `wiki trust audit`
   - `RAG export / validate`

本轮新增 live 返回样本：

6. `gbrain search 工业绿链` 已取得失败样本，当前被 `PGLite WASM runtime` 阻塞
7. `gbrain doctor --json` 已取得 live 样本，当前 `status: warnings`
8. `validate_rag_export.py` 已取得 live 样本，当前 `status: fail`
9. `validate_evidence_gates.py` 已取得 live 样本，当前 `status: pass`

## 8. 当前阻塞

1. remote 仍未返回
2. `gbrain search` 当前被 PGLite WASM 初始化故障阻塞
3. `validate_rag_export.py` 当前未通过
4. `gbrain import` live 样本仍未取得

## 9. 当前完成判定与状态上限

仅当 remote 缺口关闭，且 `ingest / search / doctor / rag export / evidence gates` 形成可归档 live 样本链后，才可判定知源专项首轮实施前验证包准备完成。

当前状态上限：

1. 最高只能保持 `partial`
2. 不得写成知识发布验证通过
3. 不得写成 `ready_for_human_acceptance`
