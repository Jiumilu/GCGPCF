---
doc_id: GPCF-F-015-ADMIN-TRIAL-20260905
title: GCWORLD管理员本机隔离试用回执
project: GPCF
status: partial
---

# GCWORLD管理员本机隔离试用回执

## 交付与授权

本会话人工批准现有admin账号、现有整包快照、24小时只读本机隔离试用。入口为 http://127.0.0.1:8710/#/gcworld/organizations 。有效期为2026-09-05T08:42:33Z至2026-09-06T08:42:33Z，即北京时间9月5日16:42:33至9月6日16:42:33。进程退出或机器休眠可能影响可用性，未建立后台调度或生产部署。

使用原开发账号的独立私有镜像与官方密码验证、登录及会话控制器，不是假身份或模拟登录响应；也不是连接原身份库持续运行的生产身份集成。原账号id=1、租户gehua、角色super_admin、业务角色operator、敏感级别S1均未改动。原身份库只读提取该账号，隔离会话更新仅在私有试用目录；原账号后续变化不会自动同步到镜像，不能声称已验证原库撤权联动。密码、密码摘要、令牌和密钥不写入本回执。

本地进程仅监听127.0.0.1:8710，接口限定为登录状态、登录、当前用户、头像、退出和GCWORLD快照读取，其余接口拒绝。授权文件每次读取须与启动时的精确授权一致；有效期不能通过替换文件延长。请求门禁与进程定时退出共同限制到期访问。未自然等待24小时，不能把边界测试表述为实际24小时运行观察。

## 实际验证

- 真实浏览器登录成功；账号错误口令被拒绝。凭据仅用于登录输入，未在命令行或交付文档中复述。
- 展示938项候选、28项项目；查找“武汉葛化物流有限公司”、核对来源版本及候选状态，未合并同名项。
- 查找葛化项目，查看完整行动、历史负责人及截止日期、相关方原始登记、历史父子项目连接与来源。历史数据没有改写为当前事实；未知时间继续未知。
- 使用规则式项目整理辅助，编辑并保存标签页本地待办草稿，界面明确未上传、未执行；没有调用外部模型或对外发送。
- 将本次试用授权临时撤回后，实际页面重新加载被拒绝且旧资料清除；恢复原精确授权与原截止时间后读取恢复，旧草稿未恢复。
- 退出登录返回登录页，再次登录并进入项目详情成功；当前交付页面保持登录，供用户试用。
- 未登录快照请求401；账号管理及任务接口403；两种静态快照路径404；登录状态200。响应带private/no-store，实际监听范围仅本机。
- 以实际授权对象检验生效前、生效时、到期前、到期时、错误租户、错误主体、空授权、错误摘要，共8项授权边界断言通过。
- 已查看实际中文页面截图。截图由本会话浏览器工具呈现；导出不受支持，没有独立图片文件，不能将旧图片声称为本次截图。

## 版本、数据与范围核对

Studio HEAD：6bbf1fab9abf3ec14cc3b7940c1f48cc461c39f8。排除本轮工作锁后，141项累计改动与LR-942验证树逐字节一致，差异0；本轮没有改产品源码。

验证树：/Users/lujunxiang/.codex/worktrees/gcworld-draft-unload-20260905 。本轮复用其构建，没有重新运行全仓测试或构建；LR-942的65条浏览器和32项定向通过属于历史适用证据，不冒充本轮新结果。

原身份库最终只读复核：admin的updated_at及last_login_at均仍为1787704868654，账号角色、租户、状态和敏感级别不变；核对查询前后文件大小、mtime及inode不变。

既有资产快照摘要：d8470e11f4dd5e7773ceb9fad92fd36a7a18dc6afcd5d7aad70ab6779f0b16fa。
既有提及资料摘要：3d1a265e7e6edae1729c93d62a8f07ae3f55d5551d0c94ad9ae78b231c01bdcc。
数据范围仍为938候选、28项目、17项相关方历史登记、2条历史父子连接，新增真实资料和关系0。

试用目录：/Users/lujunxiang/.codex/gcworld-admin-trial-PaxX2hyr，目录权限700、隔离身份文件权限600。不得将该目录作为可公开分享的产物，尤其不得公开身份数据库。
运行包装脚本trial.cjs摘要：4ff9fba5ab117272e2ce53330d18c9fe8c7e9c31c4918d8bffb3175b9e2857b2。
授权文件authorization.json摘要：eedb8bdb65b9f2294f83db5466092509051ae8521adeddfe4ac16f77d9418935。

首次启动因编译根目录及中间件包名不匹配失败，仅修正本轮私有包装脚本后启动；没有安装依赖、修改原项目或加载生产服务。文档门禁通过不等于运行试点全部验收。源码变更流程与新增产品构建本轮不适用（waived：仅现有产物的隔离运行组合）；实际读取与失效流程已单独验证。

## 已暴露的业务缺口与后续

1. 未登录访问组织世界时，登录流程丢失原目标，默认进入会话页；本次通过明确入口进入。下一开发优先修复登录后的目标保留。
2. 通用会话侧栏出现示例任务且相应服务不可用；这些不是试用业务记录，应避免与真实工作台混淆。
3. 候选列表存在代词、泛称及提取片段等噪声。后续只用现有获准快照建立可解释筛选，不删除原记录或自行确认身份。

真实关系补充、第二真实版本、多用户验收及生产身份联动仍未完成；任务6.14.3不勾选。总体保持active/partial/not_complete。

## 边界与回滚

无UNKNOWN及新增受限正文读取、KDS或真实业务写入、身份合并、事实提升、原账号权限修改、生产服务操作、提交、推送、部署或状态提升。回滚仅限撤销本次私有授权并结束本次独立进程，不停止任何既有服务、不删除原账号或原数据。试用结束不应自行续期。

官方OpsX锁用于本轮受控操作，最终释放及文件不存在以本会话官方工具回执为准。进程仅维持获准试用，不因释放工作锁自动停止。
