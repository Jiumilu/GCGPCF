---
doc_id: GPCF-DOC-C434D4038A
title: 宪衡专项运行前样本收集记录
project: WAES
related_projects: [WAES]
domain: evidence
status: controlled
version: v1.0
owner: WAES
kds_space: 开发
kds_path: 开发/04-WAES/08-evidence-samples/宪衡专项运行前样本收集记录.md
source_path: 08-evidence-samples/宪衡专项运行前样本收集记录.md
sync_direction: bidirectional
last_reviewed: 2026-06-24
supersedes: []
superseded_by: []
---

# 宪衡专项运行前样本收集记录

日期：2026-06-08
专项：宪衡
阶段：首轮实施前验证准备
当前状态：`in_progress`

## 1. 本轮目标

围绕 WAES 治理与控制塔域，收集以下 5 类运行前样本的第一批实现侧证据：

1. `EvidenceRecord`
2. `StatusAudit`
3. `AgentToolGrant`
4. 连接器治理
5. 规则/审批门禁

## 2. 已执行只读动作

本轮已执行：

1. Git 预检：
   - `pwd`
   - `git branch --show-current`
   - `git status --short --branch`
   - `git remote -v`
2. 关键对象检索：
   - `rg 'EvidenceRecord|StatusAudit|AgentToolGrant|connector|governance|approval|audit|status gate|rule|policy|grant|evidence'`
3. 关键文件抽样：
   - `src/app/domain/agent.ts`
   - `src/app/domain/scenario-governance.ts`
   - `src/app/connectors/registry.ts`

## 3. 已确认样本落点

### 3.1 Agent 权限与证据对象

已确认文件：

1. `src/app/domain/agent.ts`

已确认内容：

1. `AgentRun`
2. `AgentToolContract`
3. `AgentApprovalRequest`
4. `AgentEvidenceRecord`
5. `AgentEvidenceRecordedEvent`
6. `AgentToolRequestedEvent`
7. `AgentToolCompletedEvent`
8. `AgentToolFailedEvent`

已确认的门禁与策略字段：

1. `approvalPolicy`
2. `evidencePolicy`
3. `fallbackPolicy`
4. `requiredPermissions`
5. `externalReceiptPolicy`
6. `AgentToolCallStatus = requested | waiting_approval | running | completed | failed | blocked`

### 3.2 治理与发布边界

已确认文件：

1. `src/app/domain/scenario-governance.ts`

已确认内容：

1. `ScenarioGovernanceManifest`
2. `ScenarioGovernanceCheck`
3. `ScenarioGovernanceReleaseRecord`
4. `ScenarioGovernanceRuntimeFeedback`
5. `ScenarioGovernanceView`

已确认的治理关键字段：

1. `governanceOwner`
2. `runtimeOwner`
3. `signatureStatus`
4. `releaseStatus`
5. `evidenceLedgerStatus`
6. `businessNodeCoverage`

### 3.3 连接器治理

已确认文件：

1. `src/app/connectors/registry.ts`
2. `src/app/connectors/connectorRegistryRepository.ts`

已确认内容：

1. `connectorRegistry`
2. `ConnectorContract`
3. `ConnectorRuntimeSnapshot`
4. `ConnectorStatusView`
5. `readonlyReason`
6. `dispatch` 在当前种子实现下默认返回只读阻断结果

已确认的连接器治理信号：

1. 当前 WAES 内已有连接器注册表和运行快照抽象
2. 当前连接器模型明确保留 `readonly` 与 `readonlyReason`
3. 当前连接器能力至少支持状态读取和摘要读取，不支持无边界写入

## 4. 当前已取得的判断

1. WAES 不是只有控制塔页面，已经存在治理对象、证据对象、审批策略、连接器治理的实现侧领域模型。
2. `AgentEvidenceRecord`、审批请求、连接器只读边界、场景治理 manifest 当前都能在仓库中找到明确落点。
3. WAES 可以进入下一轮更细的样本抽取，不需要再停留在“只有架构概念”的阶段。

## 5. 当前仍未取得的样本

1. 真实 `StatusAudit` 命名对象或其等价实现落点
2. `AgentToolGrant` 明确命名对象或其等价实现落点
3. 真实 `EvidenceRecord` 样本数据
4. 真实审批请求样本数据
5. 真实连接器运行回执样本

## 5.1 本轮新增等价实现与入口

### A. `StatusAudit` 等价实现

虽然当前仓库中没有直接命名为 `StatusAudit` 的对象，但已确认以下等价实现侧入口：

1. `src/app/domain/scenario-governance.ts`
   - `ScenarioGovernanceCheck`
   - `preflightChecks`
   - `ScenarioGovernanceRuntimeFeedback`
   - `evidenceLedgerStatus`
2. `src/app/domain/agent.ts`
   - `Read runtime feedback`
   - `AgentRunStartedEvent`
   - `AgentRunCompletedEvent`
   - `AgentRunFailedEvent`
3. `src/app/agent-tools/readonlyTools.ts`
   - 运行反馈只读入口
4. `src/app/modules/isaos/scenarios/industrial-green-chain/pages/GovernanceCenterPage.tsx`
   - 治理检查和运行反馈展示入口

当前判断：

- WAES 的 `StatusAudit` 更接近“`preflightChecks + runtimeFeedback + audit repository` 组合实现”，而不是单一命名对象。

### B. `AgentToolGrant` 等价实现

虽然当前仓库中没有直接命名为 `AgentToolGrant` 的对象，但已确认以下等价实现侧入口：

1. `src/app/domain/agent.ts`
   - `requiredPermissions`
   - `approvalPolicy`
   - `AgentCapabilityGateDecision`
   - `AgentToolContract`
2. `src/app/agent-runtime/capabilityCatalog.ts`
   - 能力目录校验
   - 风险级别与审批策略校验
   - `blocked` / `approvalPolicy` / `requiredPermissions`
3. `src/app/data/mock/mockAgentCapabilities.ts`
   - 能力与权限策略种子数据
4. `src/app/modules/agent-desktop/AgentDesktopModule.tsx`
   - approval policy 展示与能力可视化入口

当前判断：

- WAES 的 `AgentToolGrant` 当前更接近“`requiredPermissions + approvalPolicy + capability gate` 组合实现”，而不是独立授权对象表。

### C. 审批请求与证据 API 入口

本轮新增确认：

1. `src/app/audit/agentAuditContract.ts`
   - `createEvidence: /waes/api/v1/agent/evidence`
   - `searchEvidence: /waes/api/v1/agent/evidence`
   - `createApprovalRequest: /waes/api/v1/agent/approval-requests`
   - `decideApprovalRequest: /waes/api/v1/agent/approval-requests/{approvalRequestId}`
   - `searchAudit: /waes/api/v1/agent/audit/search`
2. `src/app/audit/agentEvidenceLedgerRepository.ts`
   - `approvalRequests`
   - remote/local persistence status
   - `remote_api` 状态说明
3. `src/app/agent-runtime/draftApprovalRuntime.ts`
   - `waiting_approval`
   - `blocked`
   - approval request create / decide / expire / not executable 处理链

### D. 连接器运行回执与阻断入口

本轮新增确认：

1. `src/app/bus/commandBus.ts`
   - 明确存在 `blocked` 状态
   - 明确存在“Connector dispatch is blocked by readonly policy”语义
2. `src/app/bus/eventBus.ts`
   - 存在 `blocked | accepted | running` 事件状态
3. `src/app/domain/agent.ts`
   - `connector.getStatus`
   - `connector.getSummary`
   - `Read runtime feedback`

## 6. 当前阻塞

1. 当前样本仍以领域模型和注册表抽象为主，还没有真实运行记录样本。
2. 当前工作区存在既有 dirty state，后续仍必须坚持只读或受控动作。

## 7. 下一步

下一步建议继续只读抽取：

1. `StatusAudit` 等价实现落点
2. `AgentToolGrant` 等价实现落点
3. Evidence / approval / connector runtime 的 mock、fixture、repository 或 API 入口
4. 抽取可直接执行的验证入口清单，而不是继续做泛搜索

## 8. 本轮新增：证据与审批持久化样本结构

本轮新增确认文件：

1. `src/app/audit/agentEvidenceLedgerRepository.ts`
2. `src/app/workbench/WorkbenchHome.tsx`

本轮新增确认：

1. `AgentEvidenceLedgerState` 已明确包含：
   - `sessions`
   - `messages`
   - `runs`
   - `toolCalls`
   - `approvalRequests`
   - `evidence`
2. `AgentEvidenceLedgerPersistenceSource` 已明确区分：
   - `remote_api`
   - `remote_configured`
   - `local_storage`
   - `empty`
3. `createApprovalRequest()` 已确认审批请求样本结构，包含：
   - `riskLevel`
   - `targetAction`
   - `impactScope`
   - `approverPolicy`
   - `evidenceIds`
   - `expiresAt`
4. `createEvidence()` 已确认 evidence 样本结构，包含：
   - `type`
   - `source`
   - `summary`
   - `contextRefs`
   - `hash`
   - `relatedIds`
   - `inputHash`
   - `outputHash`
   - `failureReason`
   - `visibilityPolicy`
5. `persistWithRemote()` 已确认远端写入 + 本地回退语义
6. `searchEvidence()` 已确认 evidence 检索与 `totalCount/latestEvidenceAt/byType` 汇总结构
7. `WorkbenchHome.tsx` 已确认 audit read/write source 的展示入口

已新增独立文档：

1. [宪衡专项证据与审批持久化样本结构](./宪衡专项证据与审批持久化样本结构.md)

当前判断：

- WAES 在 evidence / approval / audit 这条链上，已经不仅有对象与 API，也有持久化、查询、远端回退和 UI 展示结构。
- 当前继续缺的不是结构，而是真实返回样本。

## 9. 本轮新增：审批阻断链与连接器状态链样本

本轮新增确认文件：

1. `src/app/agent-runtime/draftApprovalRuntime.ts`
2. `src/app/bus/commandBus.ts`
3. `src/app/bus/eventBus.ts`

本轮新增结论：

1. 高风险工具执行链已明确为：
   - `toolCall(waiting_approval)`
   - `draftEvidence(tool_call)`
   - `approvalRequest(pending)`
   - `approvalEvidence(approval)`
   - `toolCall update(waiting_approval)`
2. 审批决定链已明确支持：
   - `approved`
   - `rejected`
   - `expired -> blocked`
   - `not executable -> blocked`
3. connector 调度链已明确为：
   - `connector.dispatch.requested`
   - `connector.dispatch.receipt(status=blocked, readonly=true)`
   - `runtime.feedback.received(status=degraded)`
4. `eventBus.ts` 已保留：
   - `blocked`
   - `accepted`
   - `running`
   三种 receipt 状态位

已新增独立文档：

1. [宪衡专项审批阻断与证据写入链样本](./宪衡专项审批阻断与证据写入链样本.md)
2. [宪衡专项连接器阻断与状态链样本](./宪衡专项连接器阻断与状态链样本.md)
