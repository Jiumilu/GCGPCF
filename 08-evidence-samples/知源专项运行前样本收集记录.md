# 知源专项运行前样本收集记录

日期：2026-06-08  
专项：知源  
阶段：首轮实施前验证准备  
当前状态：`in_progress`

## 1. 本轮目标

围绕 KDS 知识主存与知识引擎域，收集以下 5 类运行前样本的第一批证据：

1. remote 证据
2. ingest 入口
3. 引用回指入口
4. 失效拦截入口
5. A19 对应验证入口

## 2. 已执行只读动作

本轮已执行：

1. Git 预检：
   - `pwd`
   - `git status --short --branch`
   - `git remote -v`
   - `git branch --show-current`
2. 关键文档抽样：
   - `AGENTS.md`
   - `GBRAIN_MIGRATION.md`
   - `docs/harness/acceptance.md`
   - `_rules/RAG_ADMISSION_RULES.md`
3. 关键脚本抽样：
   - `_governance/scripts/rag_admission_policy.py`
   - `_governance/scripts/wiki_trust_audit.py`
   - `_governance/scripts/validate_rag_export.py`
   - `_governance/scripts/validate_evidence_gates.py`

## 3. 本轮已确认事实

### 3.1 remote 证据

已确认：

1. 当前仓库为真实 Git 仓库
2. 当前分支为 `master`
3. 本轮 `git remote -v` 没有返回内容

当前判断：

- remote 缺口不是“待核实”，而是当前**无 remote 返回证据**。
- 在没有新的 live 证据前，不得写成已绑定远端。

### 3.2 知识真源与引擎边界

已确认文件：

1. `AGENTS.md`
2. `GBRAIN_MIGRATION.md`

已确认结论：

1. Markdown + Git 仍是 source of record
2. `/Users/lujunxiang/.gbrain/brain.pglite` 是 derived retrieval/index layer
3. 不得把 Brain search 输出直接当 canonical（KDS 才是 canonical source）
4. Brain migration 文档明确：
   - Brain 是 retrieval and graph index layer
   - 不是唯一知识真源

### 3.3 ingest 入口

已确认文件：

1. `AGENTS.md`

已确认命令入口：

1. `gbrain import . --no-embed`
2. `gbrain extract links --source db`
3. `gbrain extract timeline --source db`

当前判断：

- Brain 已明确存在本地 ingest / refresh 入口。
- 当前 ingest 样本入口已形成，但还没有本轮新的 live 返回样本。

### 3.4 引用回指与 RAG 准入入口

已确认文件：

1. `_governance/scripts/rag_admission_policy.py`
2. `_governance/scripts/wiki_trust_audit.py`
3. `_governance/scripts/validate_rag_export.py`
4. `_rules/RAG_ADMISSION_RULES.md`

已确认规则：

1. 默认准入必须同时满足：
   - `data_layer: canonical`
   - `rag_include: true`
   - `verification_status` 可准入
   - `evidence_level >= E3`
   - `source_refs` 非空
2. `validate_rag_export.py` 会校验：
   - admission json
   - allowlist txt
   - ingest manifest jsonl
   - content sha256
   - byte_size

当前判断：

- Brain 已存在引用回指与准入导出校验链。
- “回指”当前不是概念，而是 allowlist / manifest / sha256 对齐校验。

### 3.5 失效拦截入口

已确认文件：

1. `_governance/scripts/validate_evidence_gates.py`
2. `_rules/RAG_ADMISSION_RULES.md`

已确认规则：

1. `raw/working/archive` 默认排除
2. `verification_status: ai_generated/ai_extracted` 不得进入强判断
3. `evidence_level: E0/E1/E2` 只能作为线索
4. evidence intake gate 缺 frontmatter / 缺必需 section / type 不符时会 fail
5. gate 页面若未显式声明 blocked/passed，会告警

当前判断：

- Brain 当前已具备知识失效/不准入拦截入口。
- 当前还缺的是本轮 live gate 执行返回样本。

### 3.6 A19 对应入口

已确认文件：

1. `docs/harness/acceptance.md`

已确认入口：

1. `gbrain search`
2. `gbrain doctor --json`
3. `wiki trust audit`
4. `RAG export / validate`
5. `gbrain_smoke_check.sh`

当前判断：

- A19 在 Brain 域不是单一脚本，而是 doctor / search / trust audit / RAG export 校验的组合入口。

## 4. 当前已取得的判断

1. 知源专项的 remote 缺口当前仍然真实存在。
2. ingest 入口已明确。
3. 回指与 RAG 准入校验链已明确。
4. 失效拦截入口已明确。
5. A19 组合验证入口已明确。

## 5. 本轮新增 live 返回样本

### 5.1 `gbrain search` live 返回样本

已执行：

- `gbrain search 工业绿链`

结果：

1. 命令退出码为 `1`
2. 当前不是“无入口”，而是运行失败
3. 当前失败原因已明确：
   - `PGLite failed to initialize its WASM runtime.`
   - 最常见原因指向 `macOS 26.3 WASM bug`

当前判断：

- `gbrain search` 的 live 样本已取得，但当前是失败样本。
- 该失败样本可作为后续知源专项运行前阻塞证据。

### 5.2 `gbrain doctor --json` live 返回样本

已执行：

- `gbrain doctor --json`

结果：

1. 当前返回 `status: warnings`
2. 当前返回 `health_score: 95`
3. 当前告警重点不是索引不存在，而是：
   - `Could not connect to configured DB`
   - 当前只完成 filesystem checks

当前判断：

- `doctor` live 样本已取得。
- 当前 DB 连接能力仍不足，不能把 doctor 结果写成运行健康通过。

### 5.3 `validate_rag_export.py` live 返回样本

已执行：

- `python3 _governance/scripts/validate_rag_export.py ...`

结果：

1. 当前返回 `status: fail`
2. `allowlist_count: 125`
3. `manifest_count: 125`
4. `error_count: 181`
5. `warning_count: 1`
6. 当前首批错误为 `missing_file`
7. 当前明显缺失的文件样本包括：
   - `DATA_QUALITY_RULES.md`
   - `HERMES_OUTPUT_RULES.md`
   - `RAG_ADMISSION_RULES.md`

当前判断：

- RAG export live 样本已取得。
- 当前不是“校验脚本不存在”，而是校验失败。
- 失败原因当前主要表现为导出/路径/清单不一致与缺文件。

### 5.4 `validate_evidence_gates.py` live 返回样本

已执行：

- `python3 _governance/scripts/validate_evidence_gates.py ...`

结果：

1. 当前返回 `status: pass`
2. `gate_count: 46`
3. `pass_count: 46`
4. `fail_count: 0`
5. `issue_count: 0`
6. `warning_count: 0`

当前判断：

- evidence gate live 样本已取得。
- 知源专项的“失效拦截入口存在且本轮校验通过”已经可证。

## 6. 当前仍未取得的样本

1. 本轮新的 `gbrain import` live 返回样本
2. remote 绑定证据
3. RAG export fail 的修复后成功样本
4. `gbrain search` 在当前环境下的成功返回样本

## 7. 下一步

建议继续按这个顺序推进：

1. 抽 `gbrain import` 的 live 返回样本
2. 若 remote 仍为空，则把 remote 缺口保留为阻塞，不伪造关闭
3. 判断 `gbrain search` 的 WASM 故障是环境阻塞还是可绕过问题
4. 判断 `validate_rag_export.py` 的失败是当前预期阻塞还是需要立即修复的入口缺陷
