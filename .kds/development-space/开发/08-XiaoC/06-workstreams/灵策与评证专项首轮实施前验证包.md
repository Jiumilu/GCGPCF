---
doc_id: GPCF-DOC-5BEBE02506
title: 灵策与评证专项首轮实施前验证包
project: XiaoC
related_projects: [GFIS, WAES, KDS, XiaoC, XGD, XiaoG]
domain: workstreams
status: controlled
version: v1.0
owner: XiaoC
kds_space: 开发
kds_path: 开发/08-XiaoC/06-workstreams/灵策与评证专项首轮实施前验证包.md
source_path: 06-workstreams/灵策与评证专项首轮实施前验证包.md
sync_direction: bidirectional
last_reviewed: 2026-06-12
supersedes: []
superseded_by: []
---

# 灵策与评证专项首轮实施前验证包

日期：2026-06-08
专项：灵策与评证
范围：XiaoC（蚁后）+ Hermes/XGD（大象）+ XiaoG AI 服务与审计验证联合域
当前状态：`in_progress`

## 1. 当前验证目标

1. 抽取 Agent 权限、AISuggestion、模型配置、工具调用、越权拦截的实现落点
2. 明确“AI 建议不写主账”的验证入口
3. 明确 XiaoC/Hermes/XGD 的联合运行前样本要求
4. 建立 `A21-A24` 模型治理验证入口与样本清单

## 2. 当前边界

1. 本包覆盖 XiaoC（蚁后）、Hermes/XGD（大象） 的 AI 服务域与审计验证联合域。
2. 本包不把 AI 建议写成业务事实。
3. 本包不把运行前样本准备写成越权拦截已经通过。

## 3. 当前工作环境

1. 项目仓库环境：`/Users/lujunxiang/Projects/GlobalCloud XiaoC`
2. 项目仓库环境：`/Users/lujunxiang/Projects/hermes-webui`
3. 项目仓库环境：`/Users/lujunxiang/Projects/GlobalCloud XGD`
4. 本地开发运行环境
5. 设计控制环境：`/Users/lujunxiang/Documents/GlobalCloud智慧工厂`

## 4. 推荐工具与技能

推荐工具：

1. Shell
2. Git
3. Browser
4. Computer Use
5. 文档工具

推荐技能：

1. `computer-use:computer-use`
2. `browser:control-in-app-browser`
3. `globalcloud-harness-governance`

## 5. 验证方法

1. 反向验证法
2. 权限样本法
3. 回执样本法
4. 主账无写入证据法

## 6. 当前必收样本

1. AgentToolGrant 样本
2. AISuggestion 样本
3. 模型配置样本
4. 越权拦截样本
5. 主账无写入验证样本
6. `ModelAuthorizationGrant` 样本
7. `ModelRouteDecision` 样本
8. `ModelUsageRecord` / `ModelMeterSnapshot` 样本
9. `ai.model_overreach.blocked` 样本

## 6.1 `A21-A24` 验证入口

| 编号 | 验证目标 | 入口样本 | 协同专项 |
|---|---|---|---|
| A21 | 模型授权变更与生效 | `ModelAuthorizationGrant`、授权变更事件、授权前后调用对比 | 宪衡 |
| A22 | 模型路由、降级与回退 | `ModelRouteDecision`、fallback 配置、失败回退日志 | 宪衡 / 知源 |
| A23 | 模型使用计量与统计回指 | `ModelUsageRecord`、`ModelMeterSnapshot`、trace / evidence 回指 | 数枢 |
| A24 | 模型越权或超预算拦截 | `ai.model_overreach.blocked`、预算策略、无未授权调用落地证据 | 宪衡 / 数枢 |

## 7. 建议只读动作

1. 在 XiaoC 仓库继续抽取模型适配与配置落点
2. 在 Hermes 仓库继续抽取 session、task、workspace、tool call 入口
3. 在 XGD 仓库继续抽取语音、记忆、工具安全与交互验证入口
4. 在 XiaoC / Hermes 中抽取模型授权变更前后调用链
5. 抽取模型 fallback / 回退与错误处理入口
6. 抽取调用计量、token、费用或 usage 汇总入口
7. 抽取模型越权或超预算阻断入口

## 8. 当前阻塞

1. 当前仍缺运行前样本
2. Hermes/XGD 当前多为实现入口和说明证据，未形成统一回执样本
3. 模型授权、模型路由样本还未形成统一返回结构
4. 模型越权或超预算阻断样本还未形成可回指证据

## 8.1 当前已取得样本

1. `A23` 结构样本已确认：
   - `insights` 统计入口
   - session index usage 字段
   - `state.db.sessions` usage 字段
   - usage 同步入口
2. 已形成文档：
   - [A23 模型使用计量与统计回指样本.md](/Users/lujunxiang/Documents/GlobalCloud智慧工厂/A23%20模型使用计量与统计回指样本.md:1)

## 9. 当前完成判定与状态上限

仅当 9 类 AI 服务运行前样本都形成“实现落点 + 验证入口”，且 `A21-A24` 均形成样本清单后，才可判定灵策与评证专项首轮实施前验证包准备完成。

当前状态上限：

1. 最高只能保持 `partial`
2. 不得写成 AI 治理验证通过
3. 不得写成 `ready_for_human_acceptance`
